import { ModosNombrePipe } from './modos-nombre.pipe';

describe('ModoNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new ModosNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
