import { PreinscripcionesNombrePipe } from './preinscripciones-nombre.pipe';

describe('PreinscripcionesNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new PreinscripcionesNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
