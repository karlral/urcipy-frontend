import { ActivosNombrePipe } from './activos-nombre.pipe';

describe('ActivosNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new ActivosNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
